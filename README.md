# Rooney Law Email Signature Assets

This repository hosts the public Rooney Law wordmark used in the firm’s email signature. The file is intentionally public so email recipients can retrieve the logo when messages are received.

The signature should reference the direct raw HTTPS file URL, not the GitHub page URL. Do not rename or move the published PNG without first updating the central Exchange signature rule.
