<?xml version="1.0" encoding="UTF-8"?>
<!--
  Word add-in manifest for "Mail via Lob".

  This is a template. The ADDIN_ID and BASE_URL placeholders are filled in by
  the "npm run manifest" script (see docs/SETUP.md), which writes
  apps/word-addin/build/manifest.xml. Sideload that generated file, not this one.
-->
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
  xsi:type="TaskPaneApp">

  <Id>{{ADDIN_ID}}</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>Rooney Law</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Mail via Lob"/>
  <Description DefaultValue="Print and mail the open letter through Lob, including certified mail with return receipt."/>
  <IconUrl DefaultValue="{{BASE_URL}}/addin/assets/icon-64.png"/>
  <HighResolutionIconUrl DefaultValue="{{BASE_URL}}/addin/assets/icon-128.png"/>
  <SupportUrl DefaultValue="{{BASE_URL}}/addin/taskpane.html"/>

  <AppDomains>
    <AppDomain>{{BASE_URL}}</AppDomain>
  </AppDomains>

  <Hosts>
    <Host Name="Document"/>
  </Hosts>

  <Requirements>
    <Sets DefaultMinVersion="1.1">
      <Set Name="WordApi" MinVersion="1.1"/>
    </Sets>
  </Requirements>

  <DefaultSettings>
    <SourceLocation DefaultValue="{{BASE_URL}}/addin/taskpane.html"/>
  </DefaultSettings>

  <!--
    The add-in only reads the document; it never edits it. ReadAllDocument is
    the least-privilege level that still allows it: exporting the whole document
    as a PDF (getFileAsync) reads the entire file, which ReadDocument — scoped to
    the selection and bindings — does not permit. With ReadDocument, Word refuses
    the export with "You don't have sufficient permissions for this action".
  -->
  <Permissions>ReadAllDocument</Permissions>

  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">
    <Hosts>
      <Host xsi:type="Document">
        <DesktopFormFactor>
          <GetStarted>
            <Title resid="GetStarted.Title"/>
            <Description resid="GetStarted.Description"/>
            <LearnMoreUrl resid="Taskpane.Url"/>
          </GetStarted>

          <FunctionFile resid="Commands.Url"/>

          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <OfficeTab id="TabHome">
              <Group id="RooneyLawMail.Group">
                <Label resid="Group.Label"/>
                <Icon>
                  <bt:Image size="16" resid="Icon.16"/>
                  <bt:Image size="32" resid="Icon.32"/>
                  <bt:Image size="80" resid="Icon.80"/>
                </Icon>
                <Control xsi:type="Button" id="RooneyLawMail.TaskpaneButton">
                  <Label resid="Button.Label"/>
                  <Supertip>
                    <Title resid="Button.Label"/>
                    <Description resid="Button.Tooltip"/>
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16"/>
                    <bt:Image size="32" resid="Icon.32"/>
                    <bt:Image size="80" resid="Icon.80"/>
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>RooneyLawMail.Taskpane</TaskpaneId>
                    <SourceLocation resid="Taskpane.Url"/>
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>

    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16" DefaultValue="{{BASE_URL}}/addin/assets/icon-16.png"/>
        <bt:Image id="Icon.32" DefaultValue="{{BASE_URL}}/addin/assets/icon-32.png"/>
        <bt:Image id="Icon.80" DefaultValue="{{BASE_URL}}/addin/assets/icon-80.png"/>
      </bt:Images>
      <bt:Urls>
        <bt:Url id="Taskpane.Url" DefaultValue="{{BASE_URL}}/addin/taskpane.html"/>
        <bt:Url id="Commands.Url" DefaultValue="{{BASE_URL}}/addin/commands.html"/>
      </bt:Urls>
      <bt:ShortStrings>
        <bt:String id="Group.Label" DefaultValue="Mail"/>
        <bt:String id="Button.Label" DefaultValue="Mail via Lob"/>
        <bt:String id="GetStarted.Title" DefaultValue="Mail this letter"/>
      </bt:ShortStrings>
      <bt:LongStrings>
        <bt:String id="Button.Tooltip" DefaultValue="Export this document as a PDF and mail it through Lob — regular, certified, or certified with return receipt."/>
        <bt:String id="GetStarted.Description" DefaultValue="Open a letter and choose Mail via Lob on the Home tab to confirm the recipient and send it."/>
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
</OfficeApp>
